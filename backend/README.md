# PipelineHealer Backend

FastAPI service for PipelineHealer (agents + webhook + dashboard API).

This file exists because `backend/pyproject.toml` declares `readme = "README.md"`, and
Python packaging tooling (hatchling/pip editable installs) requires that file to be
present at build time.

For overall project documentation, see the repository root `README.md`.

